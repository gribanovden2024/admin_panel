name: admin_panel
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.2.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  flutter_svg: ^2.0.10+1
  cupertino_icons: ^1.0.6
  freezed: ^2.4.7
  dio: ^5.4.1
  retrofit: ^4.1.0
  json_annotation: ^4.8.1
  uuid: ^4.3.3
  shared_preferences: ^2.2.2

#  authorization_registration_view:
#    git:
#      url: https://gitlab.com/fittin/fittin-mobile/modul-app-v2/authorization-registration-view
#      ref: evropa
  interceptor:
    git:
      url: https://gitlab.com/fittin/fittin-mobile/modul-app-v2/interceptor
      ref: dev
  profile:
    git:
      url: https://gitlab.com/fittin/fittin-mobile/modul-app-v2/profile.git
      ref: dev
  profile_data:
    git:
      url: https://gitlab.com/fittin/fittin-mobile/modul-app-v2/profile-data.git
      ref: dev
  core_models:
    git:
      url: https://gitlab.com/fittin/fittin-mobile/modul-app-v2/core-models.git
      ref: dev
  core_interfaces:
    git:
      url: https://gitlab.com/fittin/fittin-mobile/modul-app-v2/core-interfaces.git
      ref: dev
  event_bus:
    git:
      url: https://gitlab.com/fittin/fittin-mobile/modul-app-v2/event-bus.git
      ref: dev

  get_it: ^7.6.7
  injectable: ^2.3.2
  injectable_generator: ^2.4.1
dependency_overrides:
  uuid: ^4.3.3
  dio: ^5.4.1
  retrofit: ^4.1.0

dev_dependencies:

  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.1
  retrofit_generator: 8.0.0
  build_runner: ^2.4.8
  json_serializable: ^6.7.1

flutter:
  uses-material-design: true

  assets:
    - assets/